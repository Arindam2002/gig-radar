# ── secrets & personal data (never commit) ─────────────────────────
.env
config.yaml
profile.yaml

# user data - lives locally (or in the mounted ./data volume)
jobscout.db*
data/
briefs/
study/
logs/

# python
.venv/
.venv
__pycache__/
*.pyc
.pytest_cache/

# node tooling lands later (diagram export); its deps never get committed
node_modules/

# the map's legibility gate writes the canvas out so a human can look at
# it; the numbers are the test, the picture is for the reader
tests/e2e/.map-concepts*.png

# os / editor
.DS_Store
.idea/
.vscode/

# local backups taken before study-file backfills
study.bak-*/
