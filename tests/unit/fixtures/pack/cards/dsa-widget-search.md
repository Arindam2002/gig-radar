- Q: What does the running summary buy you?
  A: It removes the inner loop, so one pass answers what two passes used to.
- Q: When does the widget scan stop being valid?
  A: When extending the window can improve the answer, the scan is no longer
    monotone and the pattern does not apply.
